"""Aggregate roots package for the analytics domain.

In DDD, an *aggregate* is a cluster of domain objects treated as a single unit
of consistency, and its *root* is the only entry point for modifying them.
This ``__init__.py`` marks the folder as a Python package and groups,
conceptually, those aggregate roots.
"""
